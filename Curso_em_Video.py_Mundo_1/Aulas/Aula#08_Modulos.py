import math
# from math import sqrt; para importar só um atributo e para usá-lo é só digitá-lo sem o "math.".
n= float(input('Digite um número: '))
print('A raiz de {} é {:.2f}.'.format(n, math.sqrt(n)))
import emoji
print(emoji.emojize('Olá Mundo:sunglasses::earth_americas:', use_aliases=True))